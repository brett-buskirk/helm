{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for Helm's main window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
